# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
/secrets/

# dependencies / build
node_modules/
dist/
.mastra/
.build/
*.tsbuildinfo
coverage/

# local data — the live agent (seeded from templates/workspace on first run) and its state; updates never touch these
*.db
*.db-*
data/
workspace/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.log
