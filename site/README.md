# Landing page

Static, self-contained (`index.html`). Deployed on Vercel with the project root set to `site/`
(no build step, output directory `.`). Preview locally: `python3 -m http.server -d site 8080`.
